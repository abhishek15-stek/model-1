# Mind Reader Quest

A lightweight, Akinator-inspired guessing game. Think of a character and answer the questions to see if the oracle can guess who it is.

## Run locally

You can open `index.html` directly in a browser, or run a simple local server for the best experience:

```bash
python -m http.server 8000
```

Then visit `http://localhost:8000/index.html`.
